const CookiePolicy = () => (
  <div className="max-w-2xl mx-auto py-12">
    <h1 className="text-3xl font-bold text-primary-600 mb-6">Cookiebeleid</h1>
    <p className="mb-4">Hier komt binnenkort ons cookiebeleid. We gebruiken alleen functionele cookies voor het functioneren van de website.</p>
    <p className="text-gray-500">Laatste update: {new Date().toLocaleDateString()}</p>
  </div>
);

export default CookiePolicy; 